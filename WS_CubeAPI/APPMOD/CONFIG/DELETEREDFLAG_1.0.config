DECLARE @CC_TIMESTAMP DATETIME
SET @CC_TIMESTAMP=GETDATE()
UPDATE REDFLAGS SET STATUS = 2,UPDATEDBY=@UPDATEDBY,UPDATEDDATE=@CC_TIMESTAMP WHERE REDFLAGID IN (SELECT RESULT FROM DBO.CSVTOTABLE(@REDFLAGID))
SELECT 1
INSERT INTO dbo.AUDIT(TABLENAME,TRANS,STAMP,LASTUPDATEDBY,DATA)
SELECT 'REDFLAGS','DELETE-DELETEREDFLAG_1.0',@CC_TIMESTAMP,@UPDATEDBY,NULL

